version: '3'
services:
    app:
        build:
            context: .
            dockerfile: ./Dockerfile-app.test
        ports:
            - 8080:8080
        volumes:
            - ./e2e-logs:/opt/orbs/logs
